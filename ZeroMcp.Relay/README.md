# ZeroMcp.Relay Tool Project

This project contains the `mcprelay` executable and will host:
- CLI command handling
- OpenAPI ingestion
- MCP transport (HTTP and stdio)
- Relay dispatch to external APIs
